import { createUseStyles } from 'react-jss';
import * as variables from '../../../styles/variables';
import { IconProps, CustomTheme, RuleNames } from "../interface";

const useStyles = createUseStyles<RuleNames, IconProps, CustomTheme>({

})

export default useStyles;